const router = express.Router();

//Create Order - /api/v1/order 
exports.createOrder = async (req, res, next) => {
    

    res.json(
        {
            success:true,
            order
        }
    )
}